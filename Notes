MCP - Model Context Protocol 

MCP acts as the standardized communication layer between an AI application/LLM and external capabilities such as tools and resources.
It allows AI applications or LLMs to access external tools, resources and prompts.
It contains three main components which is Host, Client and server.
Server hanldes the data acess and tools by connecting database, api's or tool.
Clients act as communication bridge between a host and server that converts a requests into structured format.
Host coordinatee communication between multiple clients and servers, and handle orchestration logic for end to end task processing.
MCP uses JSON-RPC 2.0 messages for communication between an MCP client and an MCP server.


Rosources: geeksforgeeks, databricks and MCP

Yesterday's MCP learning Update - 02-09-2026



Built an MCP client using ClientSession.
Created an MCP server using Python.
Added and tested 3 MCP tools: add_numbers, calculate, search_customers.
Created a SQLite database and connected it with the MCP server.
Added a customer resource using customer://{customer_id}.
Added and tested the analyze_customer prompt.
Built an MCP client.py using ClientSession.
Connected the client to the MCP server using stdio transport.
Tested tool calling, resource reading, and prompt retrieval from the client.
Tested tool schemas to understand their input parameters.
Learned how to read MCP Resources and retrieve Prompts.
Started understanding tool schemas and how they can be used with an LLM.
Successfully tested the complete Client → MCP Server → Database flow.

              client.py
                 │
                 │ call_tool()
                 ↓
          MCP ClientSession
                 │
                 │ MCP message
                 ↓
             server.py
                 │
                 ↓
        search_customers()
                 │
                 ↓
            database.py
                 │
                 ↓
             SQLite
                 │
                 ↓
          Customer 1001
                 │
                 ↓
          MCP response
                 │
                 ↓
             client.py



Here’s the update on yesterday’s MCP hands-on work: 03-09-2026

Integrated the Groq LLM with our MCP client.
Implemented LLM-based tool selection, allowing the model to decide which MCP tool should be used based on the user’s request.
Dynamically converted the MCP tool definitions and input schemas into the format required by Groq.
Implemented the complete LLM → MCP Tool → Tool Result → LLM workflow.
Added support for passing MCP tool results back to the LLM to generate the final response.
Improved tool input validation using structured schemas, including allowed operations for the calculation tool.
Added handling for MCP tool errors so that errors can be returned to the LLM instead of causing the client to fail.
Implemented an initial agent loop that allows the LLM to continue processing until it no longer requests a tool.
Tested the workflow with customer search and calculation queries, including combined requests.

Current status: The basic LLM + MCP agent workflow is working successfully.
Next, I will focus on improving multi-tool execution and making the agent flow.



                    User Query
                        │
                        ▼
                  ┌───────────┐
                  │  Groq LLM │
                  └─────┬─────┘
                        │
                 Select MCP Tool
                        │
                        ▼
                ┌──────────────┐
                │  MCP Client  │
                └──────┬───────┘
                       │
                  MCP Protocol
                       │
                       ▼
                ┌──────────────┐
                │  MCP Server  │
                └──────┬───────┘
                       │
                Execute Tool
                       │
              ┌────────┴────────┐
              ▼                 ▼
        SQLite Database     Other Logic
              │                 │
              └────────┬────────┘
                       ▼
                  Tool Result
                       │
                       ▼
                ┌──────────────┐
                │  MCP Client  │
                └──────┬───────┘
                       │
                 Result to LLM
                       │
                       ▼
                  ┌───────────┐
                  │  Groq LLM │
                  └─────┬─────┘
                        │
                 Final Response
                        │
                        ▼
                       User

Here’s the update on today's MCP hands-on : 04-09-2026

MCP Client Robustness:

Why we did this

To make the MCP client reliable when the LLM sends invalid requests or when MCP tools fail.

What we implemented
Maximum iteration limit — prevents infinite agent loops.
Tool validation — checks whether the requested tool exists.
Argument validation — checks JSON, required fields, and data types.
Enum validation — allows only valid calculator operations.
MCP error handling — handles tool execution failures such as division by zero.
Tool result handling — handles structured, text, and empty results.
Duplicate-call detection — prevents executing the same tool request repeatedly.
Clean logging — clearly shows LLM and MCP activities.


Tests completed:

Unknown tool → 
Invalid/missing arguments → 
Wrong argument type → 
Invalid enum (power) → 
Division by zero → 
Structured result → 
Text result → 
Empty result → 
Duplicate tool call → 
Agent logging → 

Tommorrow:
 We will Complete the remaining edge-case test for maximum iteration protection and (Resources + LLM)
Connect the LLM with our customer://{customer_id} MCP Resource and SQLite database.




Here’s the update on today's MCP hands-on : 04-09-2026


MCP – Today’s hands-on Update

Agentic MCP Workflow

Completed the agentic workflow implementation and testing.
Tested the agent with requests that require multiple MCP tool calls.
Verified that the LLM can:
Understand the user’s request.
Select the required MCP tools.
Execute tools step by step.
Use the returned results to continue the workflow.
Provide a final response based on the tool results.

Tested multiple tasks in a single request, such as:
Customer search + calculation.
Multiple calculations.
Customer search + multiple calculations.
Tested tool failure handling using a divide-by-zero scenario.
Verified that when one MCP tool fails, the agent can continue with the remaining tasks and report the failure accurately.


Work Flow

User Request → LLM → Select MCP Tool → MCP Server → Execute Tool → Return Result → LLM → Next Tool (if required) → Final Response


Tomorrow:
I will tart MCP Agent Improvements. and will Improve MCP error propagation so tool errors are passed to the LLM more clearly.
Then will Improve the error messages and handling flow.
Finally,I will clean up the temporary test code and continue improving the agent workflow.




MCP Learning Notes — Today
1. What we did today

Added logging to clearly see LLM responses, tool requests, and MCP execution.
Created tests/test_agent.py and moved the agent tests into a separate test file.
Converted the tests to pytest and got all 6 tests passing.
Tested MCP Resource and Prompt integration with customer data.
Improved the customer analysis prompt to reduce hallucinated customer information.
Continued the customer intelligence workflow with MCP, SQLite, and Groq LLM.
Added and tested the get_customers_needing_follow_up tool.
The tool finds active customers who have not been contacted recently.
Tested it with the default 14-day value and received customers 1001 and 1002.


Connected the tool with the agent loop and tested:

Find customers who need follow-up and analyze them.

The LLM correctly selected the follow-up tool and generated analysis for both customers.
We also worked on keeping the analysis based on verified customer information.
We found that the LLM can still add general industry assumptions, so grounding needs further improvement.


2. Flow we learned today
              User Query
              ↓
              Groq LLM
              ↓
              MCP Tool
              ↓
              MCP Server
              ↓
              SQLite Database
              ↓
              Customer Data
              ↓
              MCP Client
              ↓
              Groq LLM
              ↓
              Final Analysis

Tool → performs an action or retrieves information.
Resource → provides specific customer data.
Prompt → gives instructions for analysis.
LLM → decides what action to take.
MCP Client → connects the LLM with the MCP server.


3. Test case completed today:

Test Case: Agent tool calling and validation

Tests completed:

Duplicate tool call
Unknown tool
Invalid JSON
Missing required argument
Wrong argument type
MCP tool error

Result:

6 passed
We also tested a customer analysis request.
The agent retrieved the MCP Resource and Prompt and produced a grounded response.
An adversarial test asking for unavailable customer information correctly returned that the information was not available.

Test Case: Customers needing follow-up

Input:

Find customers who need follow-up and analyze them.

Result:

1001 — Arun Kumar — TechNova Solutions
1002 — Priya Sharma — GreenLeaf Industries
The follow-up tool executed successfully.
The agent completed the task in 2 iterations.
The LLM generated analysis and recommendations for both customers.


4. Important issue we found:

LLMs can sometimes invent information that is not present in the MCP data.
Strong prompts and verified data can reduce this problem, but we need to keep testing it.
This showed why grounding and validation are important when building AI agents.

The agent works, but the LLM can mix verified customer facts with general industry information.
We need to keep customer-specific analysis based only on information available through MCP.
Facts, general observations, and recommendations should be clearly separated.


5. Tomorrow's hands-on plan:

Continue the multi-customer analysis workflow.
Improve MCP Resource retrieval for multiple customers.
Make the analysis more strongly grounded in verified data.
Test customer://1001 and customer://1002.
Add tests for the multi-customer workflow.
Make Resource retrieval more generic and reusable.



MCP Learning Notes — -08-09-2026

1. What we did today:

Added priority to the customer database.
Added days_since_contact to customer search results.
Added a follow-up score for customers.

Used this formula:
High = 3
Medium = 2
Low = 1

Follow-up Score = Priority Weight × Days Since Contact
Tested the score:
Customer 1001 → 57
Customer 1002 → 48

Created prioritize_customers() in database.py.
The function calculates the score and sorts customers from highest to lowest score.
Added prioritize_customers as an MCP tool.
Tested the tool successfully through the MCP client and Groq LLM.

Tested the query:
Who should I contact first?
The LLM correctly selected prioritize_customers and recommended Customer 1001.
Added a days parameter to prioritize_customers().

Tested different follow-up periods:
14 days → Customers 1001 and 1002
20 days → Customer 1002 only
Fixed duplicate test output caused by incorrect indentation.
Fixed the missing newline in the customer output.

2. Flow we learned today

User Query
    ↓
Groq LLM
    ↓
MCP Tool
    ↓
MCP Server
    ↓
SQLite Database
    ↓
Business Logic
    ↓
Follow-up Score
    ↓
Customer Ranking
    ↓
MCP Result
    ↓
Groq LLM
    ↓
Final 

Database → stores customer data.
Business Logic → calculates scores and ranking.
MCP Tool → provides the business logic to the LLM.
MCP Client → connects the LLM to the MCP server.
LLM → selects the tool and explains the result.

3. Test cases completed today:

Test Case: Follow-up Score

Result:

1001 → High × 19 = 57
1002 → Medium × 24 = 48

Score calculation worked correctly.

Test Case: Customer Prioritization

Input:

Prioritize the active customers for follow-up.

Result:

1. Arun Kumar → Score: 57
2. Priya Sharma → Score: 48
Test Case: Natural Language Tool Selection

Input:

Who should I contact first?

Result:

The LLM selected prioritize_customers and recommended Arun Kumar.

Test Case: Follow-up Threshold

Input:

14 days
20 days

Result:

14 days → Arun Kumar, Priya Sharma
20 days → Priya Sharma

The threshold worked correctly.

4. Important issue we found & Solved:

Business rules should be handled by application code, not by the LLM.
The MCP server calculates the follow-up score and ranking.
The LLM uses the result and explains it to the user.
We also fixed duplicate output caused by incorrect indentation.


5. Tomorrow's hands-on plan
Expose the days parameter through the MCP tool.
Check the updated MCP tool schema.
Test prioritize_customers(days=20).
Test natural-language requests with different follow-up periods.
Verify that the LLM sends the correct days value.
Continue improving the customer prioritization workflow.